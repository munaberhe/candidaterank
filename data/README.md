# Data directory

This directory holds reference data used by CandidateRank:

- expression.csv (gene, tissue, expr_value)
- pathways.csv (gene, pathway)
- gwas.csv (gene, trait)

For v1, toy data is hard-coded in `candidaterank/data_loaders.py`.
Real data files will be added here in a later version.
